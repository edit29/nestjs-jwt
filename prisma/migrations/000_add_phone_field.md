Add phone and phoneVerified fields to User model; run `npx prisma migrate dev --name add_phone` to generate SQL migration.

Note: migration file not included here. Run the command locally to update your database.
